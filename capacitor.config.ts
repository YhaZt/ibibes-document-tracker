import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.ibibestracker',
  appName: 'ibibes-document-tracker',
  webDir: 'dist/pwa'
};

export default config;
